databases:
  - name: themis-db
    plan: free
    databaseName: themis
    user: themis
    region: oregon

services:
  - type: web
    name: themis-backend
    runtime: python
    plan: free
    region: oregon
    rootDir: backend
    buildCommand: pip install -U pip && pip install .
    startCommand: set -e && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: themis-db
          property: connectionString
      - key: ALLOWED_ORIGINS
        sync: false
      - key: PYTHONUNBUFFERED
        value: "1"
    healthCheckPath: /healthz

  - type: web
    name: themis-frontend
    runtime: static
    plan: free
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        sync: false
      - key: VITE_WS_URL
        sync: false
